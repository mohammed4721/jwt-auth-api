# jwt-auth-api
A backend authentication system built with Node.js and Express.js that uses JWT for secure login and signup. It includes middleware for authentication and role-based authorization for Admin and Student routes.
